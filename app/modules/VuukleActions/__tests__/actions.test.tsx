import 'jest';
// import * as actions from '../actions';

describe('Actions should work correctly', () => {
  test('setRecommendationsCount action is called and it sends the correct parameters', () => {
    // const spy = jest.spyOn(actions, 'setRecommendationsCount');
    // const call = actions.setRecommendationsCount(3);

    // expect(spy).toHaveBeenCalledWith(3);

    // expect(call).toHaveProperty('type', 'SET_RECOMMENDATIONS');
    // expect(call).toHaveProperty('error', undefined);
    // expect(call).toHaveProperty('payload');
    // expect(call).toHaveProperty('payload', 3);

    expect(true).toBe(true);
  });
});
